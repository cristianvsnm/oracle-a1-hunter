name: Oracle A1 Hunter

on:
  workflow_dispatch:

jobs:
  hunter:
    runs-on: ubuntu-latest

    env:
      OCI_CLI_USER: ${{ secrets.OCI_USER_OCID }}
      OCI_CLI_TENANCY: ${{ secrets.OCI_TENANCY_OCID }}
      OCI_CLI_FINGERPRINT: ${{ secrets.OCI_FINGERPRINT }}
      OCI_CLI_KEY_CONTENT: ${{ secrets.OCI_PRIVATE_KEY }}
      OCI_CLI_REGION: sa-santiago-1

    steps:

      - name: Comprobar autenticacion OCI
        uses: oracle-actions/run-oci-cli-command@v1.3.2
        with:
          command: 'iam region list'
          silent: false

      - name: Buscar Minecraft-Server
        id: check
        uses: oracle-actions/run-oci-cli-command@v1.3.2
        with:
          command: 'compute instance list --compartment-id ${{ secrets.OCI_TENANCY_OCID }} --display-name Minecraft-Server --all'
          query: 'length(data)'
          silent: false

      - name: Mostrar resultado
        run: |
          echo "=========================================="
          echo "RESULTADO DE BUSQUEDA"
          echo "=========================================="
          echo ""
          echo "raw_output:"
          echo "'${{ steps.check.outputs.raw_output }}'"
          echo ""
          echo "output:"
          echo "'${{ steps.check.outputs.output }}'"
          echo ""
          echo "=========================================="

          if [ "${{ steps.check.outputs.raw_output }}" = "0" ]; then
            echo "NO EXISTE Minecraft-Server."
            echo "Esto es lo esperado."
            echo "El Hunter debera intentar crear la A1."
          else
            echo "El resultado NO es 0."
            echo "Todavia no vamos a crear ninguna instancia."
          fi

          echo "=========================================="

      - name: Prueba terminada
        run: |
          echo ""
          echo "=========================================="
          echo "PRUEBA TERMINADA"
          echo "=========================================="
          echo ""
          echo "No se intentara crear la A1 en esta ejecucion."
          echo "Estamos comprobando la deteccion de instancias."
          echo ""
          echo "=========================================="
